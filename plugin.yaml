name: hermes-observatory
version: 0.1.0
description: "Real-time terminal dashboard (TUI) for monitoring Hermes agent health — gateway status, reliability scores, session quality, and token burn across all profiles. Launched via /observatory slash command."
author: "Ant Dev"
kind: standalone
hooks:
  - post_tool_call
  - on_session_end
